  /* ============ מסך הפתיחה: גלגל שנופל, מקפיץ, ומתגלגל ============
   הגלגל הוא הרנדר עצמו — התמונה כפי שהיא, מסתובבת. בלי מודל ובלי
   נפח מודבק. מאחורי החישוק יושב עיגול אטום בצבע הרקע, ולכן הגלגל
   הוא גוף ולא מדבקה: כשהוא עובר מול הסימן הוא חוסם אותו, והעין
   קוראת שני גופים זה לפני זה ולא שתי תמונות זו על זו.

   ברגע הפגיעה מצויר משהו אחר לגמרי: קדרי הרנדר עצמם. שם הגומי
   נמעך ומתנפח כמו במציאות, ואת זה לא מחקים — פשוט מציגים. הרצועה
   חתוכה כך שתחתית כל תא היא קו הקרקע, והמקום עליה נגזר מעומק
   המעיכה: בדרך פנימה מהתאים הראשונים, בדרך החוצה מהאחרונים.
   המקום הזה שבור — בין שני תאים — ולכן מציירים את הקרוב שבהם
   ומשלימים את השארית במתיחה סביב קו הקרקע. כך יש צורת ביניים לכל
   פריים, והמעבר חלק, אבל הפיקסלים נשארים חדים ושל הרנדר.

   העיוות חי בדיוק כל עוד יש מגע. ביציאה הוא נמדד ביחס למה שנשאר
   מהמעיכה, ולכן הגומי מתיישר בדיוק ברגע שהגלגל עוזב את הסימן —
   ואינו ממשיך להתנדנד באוויר.

   בשאר הזמן התמונה מפוצלת לשתי שכבות: גומי וחישוק. מה שנכנס מתחת לקו הקרקע
   לא מצויר, והגומי גם מתרחב הצידה לפי עומק המעיכה — נפח נשמר.
   החישוק אינו נמתח לעולם: הוא נשאר עגול ורק שוקע עם הציר, בדיוק
   כמו בגלגל אמיתי שהאוויר בו מעט חסר.

   המגע אינו החזרה מיידית אלא כוח: הצמיג הוא קפיץ עם בולם, החדירה
   שלו למשטח היא בדיוק המעיכה שרואים, וההקפצה נולדת מהכוח הזה —
   ולכן הקפיצות דועכות מעצמן ואין שום מקדם החזרה כתוב ביד. בפתח
   המגע יש חיכוך קולומב אמיתי: הוא מסובב את הגלגל, בולם החלקה,
   ומייצר את התנגדות הגלגול.

   הגלגל נופל בדיוק במרכז המסך, שהוא גם מרכז הסימן. שם המשטח
   אופקי, ולכן אין שום כוח שידחוף אותו הצידה: מה שמניע אותו הוא
   הסיבוב שאיתו הוא נכנס, כמו גלגל שהשתחרר ונפל. ברגע שהוא תופס
   משטח החיכוך הופך את הסיבוב לנסיעה, והירידה מכתר הסימן רק
   מוסיפה. אין שום דחיפה כתובה ביד.

   הסימן נותן — מסה על קפיץ עם בולם, מונע מכוח המגע עצמו. הוא קשיח
   הרבה יותר מהגומי ולכן הגלגל ניתז ממנו ולא שוקע בו, אבל הבליעה
   שלו קטנה: אחרי המכה הוא יורד, חוזר ועולה מעל למקומו, ורק אז
   נרגע. הפרוגיב אמיתי — משטח הנחיתה יורד יחד איתו.
   אחר כך הוא נוחת על הרצפה ומקפץ כמו כדור: שרשרת קפיצות שהולכות
   וקטנות, ורק אז הוא מתיישב לגלגול. זה יוצא מעצמו מהבליעה של
   הגומי, שגדלה עם קצב העיוות — מכה חזקה בולעת הרבה וקפיצה קטנה
   כמעט לא.

   האור על הגומי אינו מסתובב. בקובץ הוא היה אפוי בתמונה — גם האור
   הכיווני וגם הברק המבריק, כתם בהיר שיושב בכל קדר במקום אחר —
   וכשהתמונה מסתובבת מסתובב איתו הכול. זה הסימן הכי חזק לכך
   שמסתובבת תמונה ולא גוף, וברגע המעבר לקדרי הפגיעה הכתם קפץ.
   לכן ניקינו את הגומי בקואורדינטות קוטביות: הפרוטקטור חוזר סביב
   הגלגל כל 9 מעלות והברק נמרח על 60-90, ולכן החלקה של הבהירות
   לפי הזווית בלבד, בחלון של 25 מעלות, מפרידה ביניהם. נשאר הגומי
   עצמו (33% אי-אחידות ירדו ל-2%), וכל האור בא משכבה אחת במסך.

   שני דברים נוספים עושים את ההבדל בין תמונה שזזה לבין צילום. הראשון הוא
   טשטוש תנועה: בשיא הנפילה הגלגל עובר בין פריים לפריים כרבע
   מהקוטר שלו, ובלי מריחה העין רואה סדרת קפיצות. השני הוא הצל —
   הוא מונח תמיד על הרצפה, מתרחב ומחוויר ככל שהגלגל גבוה, ומצויר
   לפני הסימן כך שהסימן עומד לפניו ולא נמרח בו.

   בנסיעה הוא מתנודד. גלגל מתגלגל אינו נופל: הסיבוב מייצב אותו,
   והנטייה הצידה מתחלפת בהיגוי דרך פרסציה גירוסקופית — ההיגוי
   מפגר אחרי הנטייה ברבע מחזור, ולכן זה יוצא נדנוד ולא נפילה.
   תדר הנדנוד עולה עם קצב הסיבוב, וכל נחיתה מנערת אותו לצד השני.

   השם נחשף לאורך הקשת של הגלגל עצמו, מהנגיעה הראשונה ברצפה — אחרי
   הקפיצה הגדולה מהסימן, כשהוא כבר נוסע. הוא יושב מחוץ לזרימה, ולכן רוחב
   הבמה הוא הסימן בלבד והוא ממורכז כבר בציור הראשון.

   שתי התנועות מופרדות בכוונה. הבמה זזה כבר ברגע הפגיעה, בזמן
   שהגלגל באוויר: אילו הייתה זזה תוך כדי גלגול, המסך היה נגרר
   שמאלה בקצב קרוב לזה של הגלגל, הגלגל היה כמעט עומד במקום
   ומסתובב — וזו בדיוק התחושה של תמונה שמסתובבת. השם מתקרב
   אחר כך, בזמן הגלגול, וזה לא פוגע בקריאה של הנסיעה. */
(function () {
  var stage = document.getElementById('stage');
  var mark = document.getElementById('spMark');
  var word = document.getElementById('spWord');
  var wheel = document.getElementById('spWheel');   // המסגרת: מיקום במסך
  var cut = document.getElementById('spCut');        // קופסת החיתוך, לא מסתובבת
  var tyre = document.getElementById('spTyre');      // הגומי — נמעך
  var rim = document.getElementById('spRim');        // החישוק — קשיח
  var lite = document.getElementById('spLite');      // האור — אינו מסתובב
  var hit = document.getElementById('spHit');        // קדרי הפגיעה מהרנדר
  var shade = document.getElementById('spShade');
  var mblur = document.getElementById('spMBg');   // טשטוש התנועה
  /* אם ה-HTML שהגיע מהמטמון ישן יותר מהקובץ הזה, החלקים החדשים
     פשוט לא קיימים. אז עוצרים בשקט — מסך הפתיחה יישאר סטטי ויימוג
     כרגיל — במקום ליפול על אלמנט חסר ולהשאיר את הגלגל תקוע בפינה. */
  if (!stage || !mark || !word || !wheel || !cut || !tyre || !rim || !hit) return;
  if (matchMedia('(prefers-reduced-motion: reduce)').matches) return;

  var G = 1200;            // נפילה קלה יותר; עומק המעיכה תלוי בגובה, לא בו
  var SAG = 0.011;         // צמיג מנופח כמעט מלא — שוקע מעט ומחזיר חזק
  /* בליעה פנימית של הגומי לפי קצב העיוות (האנט-קרוסלי): הכוח הבולם
     גדל עם החדירה, ולכן אין קפיצת כוח ברגע הנגיעה, והוא גדל עם
     המהירות — פגיעה חזקה בולעת הרבה, וקפיצה קטנה כמעט לא. מזה יוצא
     בדיוק מה שרואים אצל כדור קופץ: מכה ראשונה בולעת, ואחריה שרשרת
     קפיצות חיות שדועכות לאט. */
  var HYST = 0.0010;
  var HDRY = 0.50;         // חיכוך פנימי שאינו תלוי בקצב
  var BULGE = 0.33;        // כמה הגומי מתרחב לרוחב ביחס לעומק המעיכה
  var VREST = 55;          // מתחת למהירות הזו המגע נחשב מנוחה
  /* משטח המגע במנוחה, ביחידות רדיוס — נמדד בקדר האחרון של הרנדר,
     זה שבו הגלגל כבר יושב על הרצפה: משטח המגע שם 33.9% מהקוטר
     והצמיג רחב ב-1.7%. זו הצורה שרואים כל זמן הגלגול. */
  var FLAT0 = 0.052;
  /* עומק המעיכה ורוחב הגומי בכל קדר של הרצועה, כפי שנמדדו ברנדר:
     העומק בחלקי רדיוס, הרוחב ביחס לגלגל שלם. ארבעת הראשונים הם
     הדחיסה, מהרביעי והלאה ההתאוששות. האחרון בכל מערך וירטואלי —
     הגומי שכבר חזר לעצמו — והוא מצויר בפיקסלים של הקדר שלפניו. */
  var HIT_N = 8;           // תאים אמיתיים ברצועה
  var HIT_D = [0.000, 0.009, 0.069, 0.112, 0.043, 0.026, 0.009, 0.009, 0.000];
  var HIT_W = [1.009, 1.000, 1.000, 1.009, 1.034, 1.060, 1.060, 1.039, 1.000];
  var HIT_MAX = 0.112;
  var HIT_ON = 0.02;       // מאיזה עומק עוברים לקדרי הרנדר
  var HIT_CELL = 168 / 143;   // יחס התא לקוטר הגלגל
  var HIT_FOOT = 3 / (143 / 233);   // כמה התא יורד מתחת לקו הקרקע, ביחידות רנדר

  /* הסימן נותן. מסה על קפיץ עם בולם, מונע מכוח המגע עצמו, ומשטח
     הנחיתה יורד יחד איתו — הפרוגיב אמיתי ולא מצויר. הוא עדיין קשיח
     הרבה יותר מהגומי, ולכן הגלגל ניתז ממנו ולא שוקע בו; אבל הבליעה
     קטנה, ולכן אחרי המכה הוא חוזר בתנודה דועכת שרואים ממש: יורד,
     עולה מעל למקומו, ורק אז נרגע. */
  var LW = 36;             // תדר הסימן, רדיאן לשנייה
  var LZ = 0.16;           // בליעה קטנה: כמה תנודות נראות ואז מנוחה
  var KL = 20000;                    // קשיחות הסימן ליחידת מסה
  var ML = KL / (LW * LW);           // המסה שנותנת בדיוק את התדר הזה
  var CL = 2 * LZ * KL / LW;         // והבולם שנותן בדיוק את הבליעה הזו
  var MU = 1.05;           // אחיזת גומי
  var RR = 0.012;          // התנגדות גלגול
  var BOTTOM = 0.24;       // מאיזו חדירה הצמיג יושב על החישוק ומתקשה
  var CROWN_SLOPE = 0.16;  // שיפוע הכתר בשוליו — מסייע לגלגל לרדת ממנו
  var BASE_OM = 7;         // קצב הסיבוב שאיתו הגלגל נכנס למסך, על מסך צר
  var OM0;                 // נגזר מאורך המסלול
  var H = 0.0012;          // צעד הסימולציה — הקפיץ קשיח, צריך צעד קצר

  var box = stage.getBoundingClientRect();
  var mb = mark.getBoundingClientRect();
  var wb = word.getBoundingClientRect();
  var R = (wheel.offsetWidth || 60) / 2;   // offsetWidth: לא מושפע מהסיבוב

  var markTop = mb.top - box.top;
  var markLeft = mb.left - box.left;
  var markCx = markLeft + mb.width / 2;
  var markH = mb.height || 1;
  var ground = mb.bottom - box.top;       // קו הרצפה של הסימן
  var wordLeft = wb.left - box.left;
  var wordW = wb.width;
  var SHIFT = wb.width / 2;
  var exitX = (window.innerWidth - box.left) + SHIFT + 60;

  var K = G / (SAG * R);                  // קשיחות הצמיג ליחידת מסה
  /* הבמה רחבה בדיוק כרוחב הסימן והיא ממורכזת, ולכן מרכז הסימן הוא
     גם מרכז המסך. הגלגל נופל בדיוק לשם. */
  var startX = markCx;
  var CROWN_HALF = mb.width * 0.42;       // מעבר לזה נגמר הסימן ונופלים ממנו
  var CROWN = CROWN_SLOPE / (2 * CROWN_HALF);   // כתר מעוגל, שיפועו נמדד בשוליים
  /* על מסך רחב יש יותר דרך לעבור, ולכן הגלגל נכנס מסתובב מהר יותר.
     זה תנאי התחלה של הסצנה ולא שינוי בדינמיקה — אחרת על מסך רחב
     הוא היה מתגלגל בנחת עוד שניות אחרי שהכול כבר עמד במקום. */
  var runway = exitX + R - startX;
  OM0 = Math.max(BASE_OM, Math.min(3.6 * BASE_OM, BASE_OM * runway / 345));

  /* התנודדות בנסיעה. גלגל מתגלגל לא נופל: הסיבוב מייצב אותו, והנטייה
     הצידה מתחלפת בהיגוי דרך פרסציה גירוסקופית — ההיגוי מפגר אחרי
     הנטייה ברבע מחזור. תדר ההתנודדות עולה עם קצב הסיבוב, כמו בגוף
     מסתובב אמיתי, והדעיכה איטית: גלגל מתנודד הרבה זמן. */
  var LEAN_D = 0.055;                     // דעיכת ההתנודדות
  var LEAN_F = 0.62;                      // תדר ההתנודדות ביחס לקצב הסיבוב
  var STEER_A = 0.75;                     // כמה ההיגוי נגרר אחרי הנטייה
  var LEAN_KICK = 1.6e-4;                 // כמה כל נחיתה מנערת אותו — עדין

  /* טשטוש תנועה: כמה מהמרחק שהגלגל עובר בפריים נמרח בתמונה. זה
     בערך צמצם של רבע פריים — מספיק כדי שהנפילה תיקרא כתנועה,
     ולא כל כך הרבה שהגלגל יהפוך לכתם. */
  var MB = 0.105, MB_MAX = R * 0.055;
  var SHH = shade ? shade.offsetHeight / 2 : 0;   // חצי גובה הצל, נמדד פעם אחת

  var RT = R * 0.98;                      // רדיוס הגומי לחשיפת האותיות
  var seen = 0, GAP = 0;
  var stageT = -1, stageDur = 1.00, stageE = 0;   // הבמה — מרגע הפגיעה
  var wordT = -1, wordDur = 1, wordE = 0;        // השם — מרגע ההתייצבות

  function sgn(v) { return v > 0 ? 1 : v < 0 ? -1 : 0; }
  /* הבמה נדחפת מהפגיעה: מהר בהתחלה ונרגעת — כמו רתע. השם מתקרב
     ברכות משני הצדדים, בלי זינוק ובלי עצירה חדה. */
  function easeOut(u) { u = Math.max(0, Math.min(1, u)); return 1 - Math.pow(1 - u, 3); }
  function easeInOut(u) {
    u = Math.max(0, Math.min(1, u));
    return u < 0.5 ? 4 * u * u * u : 1 - Math.pow(-2 * u + 2, 3) / 2;
  }

  /* חזית החשיפה עוברת במרכז הגלגל, ולכן האות יוצאת ממש מתחתיו.
     כל עוד הגלגל מקפץ אין חשיפה בכלל: הקפיצות מדלגות על אותיות. */
  function revealWord() {
    if (wordT < 0) return;
    var here = wordLeft + GAP * (1 - wordE);
    var f = Math.max(0, Math.min(wordW, x - here));
    if (f > seen) seen = f;
    word.style.transform = 'translateX(' + (GAP * (1 - wordE)).toFixed(2) + 'px)';
    word.style.clipPath = 'inset(0 ' + (wordW - seen).toFixed(1) + 'px 0 0)';
  }

  var x = startX;
  /* הגלגל נכנס לפריים כשהוא כבר נופל. אם הוא מתחיל בתוך המסך
     וממנוחה, רואים אותו נתלה רגע בקצה העליון ורק אז יוצא לדרך —
     וזה נראה כמו הופעה, לא כמו נפילה. לכן הוא מתחיל מעל הקצה
     ובמהירות, כאילו נפל כבר מגובה. */
  var y = -box.top - R - 30;
  var VY0 = 330;
  /* הוא נופל בדיוק על מרכז הכתר, ושם המשטח אופקי: אין שום כוח
     שידחוף אותו הצידה. מה שמניע אותו הוא הסיבוב שאיתו הוא נכנס,
     כמו גלגל שהשתחרר ונפל — ברגע שהוא תופס משטח, החיכוך הופך אותו
     לנסיעה, והירידה מהכתר רק מוסיפה. אין שום דחיפה כתובה ביד. */
  var vx = 0, vy = VY0, rot = 0, om = OM0;
  var md = 0, mdv = 0;                     // הכנעת הסימן ומהירותה
  var lean = 0, leanV = 0, steer = 0;      // נטייה והיגוי
  var kick = 1, wasAir = true;             // כל נחיתה מנערת לצד השני
  var vIn = 0;                             // מהירות הכניסה למגע הנוכחי
  var pen = 0, surface = ground, pastLogo = false;
  /* המגע קצר מפריים, ולפעמים קצר משני פריימים: אם נשאל על העומק רק
     ברגע הציור, מכה שלמה יכולה ליפול בין שני פריימים ופשוט לא
     להיראות. לכן הסימולציה, שרצה בצעדים קטנים בהרבה, זוכרת את
     העומק הגדול ביותר שהיה מאז הציור הקודם — ואת המשטח שבו. */
  var penPk = 0, surfPk = ground;
  var last = 0, done = false;

  /* גובה המשטח ושיפועו במקום נתון. הכתר של הסימן מעוגל: נחיתה מחוץ
     למרכזו מטה את הנורמל, וזה מה שמעיף את הגלגל הצידה. */
  function surfaceAt(px) {
    if (!pastLogo) {
      var dx = px - markCx;
      if (Math.abs(dx) < CROWN_HALF) {
        return [markTop + md + CROWN * dx * dx, 2 * CROWN * dx, 1];
      }
    }
    return [ground, 0, 0];
  }

  function sub(h) {
    vy += G * h;
    x += vx * h; y += vy * h; rot += om * h;
    if (!pastLogo && x - markCx > CROWN_HALF) pastLogo = true;

    var s = surfaceAt(x);
    var ys = s[0], sl = s[1], onLogo = s[2];
    pen = (y + R) - ys;
    var Fn = 0;
    if (pen > 0) {
      var inv = 1 / Math.sqrt(1 + sl * sl);
      var nx = sl * inv, ny = -inv;          // נורמל היוצא מהמשטח
      var tx = -ny, ty = nx;                 // משיק, לכיוון ימין
      var vn = vx * nx + vy * ny;            // שלילי כשנכנסים פנימה
      if (wasAir) { kick = -kick; wasAir = false; vIn = Math.abs(vn); }
      var q = pen / (BOTTOM * R);
      /* שתי בליעות. הראשונה תלויה בקצב העיוות ולכן בולעת מכה חזקה.
         השנייה היא חיכוך פנימי של הגומי שאינו תלוי בקצב: הוא מוסיף
         כוח בכניסה וגורע ביציאה, ולכן לוקח שבר קבוע מהאנרגיה בכל
         מגע — וזה מה שמחסל את שרשרת הקפיצות הקטנות. בלעדיו קפיצה
         איטית כמעט לא נבלעת והגלגל מרטט עד אין סוף. */
      Fn = K * pen * (1 + 20 * q * q * q * q) * (1 - HYST * vn - HDRY * sgn(vn));
      if (Fn < 0) Fn = 0;                                          // משטח לא מושך
      var vt = vx * tx + vy * ty - om * R;   // החלקה בפתח המגע
      var Ft = -sgn(vt) * Math.min(MU * Fn, Math.abs(vt) / (2 * h));
      vx += (Fn * nx + Ft * tx) * h;
      vy += (Fn * ny + Ft * ty) * h;
      om -= Ft / R * h;                      // אותו כוח מסובב את הגלגל
      var rr = RR * Fn / R * h;              // התנגדות גלגול, דרך המומנט
      om -= sgn(om) * Math.min(Math.abs(om), rr);
      /* מגע מנוחה. הבליעה של הגומי גדלה עם קצב העיוות, ולכן נגיעה
         איטית מאוד כמעט לא נבלעת והגלגל היה רועד בלי סוף בשבריר
         פיקסל. המבחן הוא על מהירות הכניסה למגע ולא על המהירות
         הרגעית — אחרת הוא היה מאפס גם את שיא הדחיסה של פגיעה חזקה
         ומבטל את ההקפצה עצמה. */
      if (vIn < VREST) { vx -= vn * nx; vy -= vn * ny; }
      if (onLogo && stageT < 0) stageT = 0;   // מרגע הפגיעה הבמה זזה
      // רק מכה של ממש מנערת את הגלגל; המשקל השוטף לא
      if (Fn > 4 * G) leanV += (Fn - 4 * G) * LEAN_KICK * h * kick;
    } else {
      pen = 0;
      wasAir = true;
    }
    surface = ys;
    if (pen > penPk) { penPk = pen; surfPk = ys; }

    // הסימן: מסה על קפיץ, מונע מכוח המגע ומתאושש בתנודה דועכת
    mdv += ((onLogo ? Fn : 0) - KL * md - CL * mdv) / ML * h;
    md += mdv * h;
    /* נותנים לו להתנדנד עד הסוף — זה הפרוגיב שרואים. עוצרים רק
       כשהוא באמת עמד. */
    if (Math.abs(md) < 0.01 && Math.abs(mdv) < 0.4) { md = 0; mdv = 0; }

    var ws = Math.max(2.5, Math.abs(om) * LEAN_F);
    leanV += (-ws * ws * lean - 2 * LEAN_D * ws * leanV) * h;
    lean += leanV * h;
    if (lean > 0.2) { lean = 0.2; leanV = 0; }
    if (lean < -0.2) { lean = -0.2; leanV = 0; }
    steer = -(leanV / ws) * STEER_A;

    if (wordT < 0 && pen > 0 && pastLogo && vIn < 200) {
      /* הנגיעה הרכה הראשונה ברצפה: הקפיצות כבר נגמרו והגלגל נוסע.
         המבחן הוא על מהירות הכניסה למגע — נחיתה חזקה עוד אומרת
         שיש קפיצה אחריה, ואז אין טעם לפתוח אותיות. מכאן
         והלאה הגלגל כבר נוסע — הקפיצות שנשארו נמוכות. השם מונח כך
         שקצהו השמאלי בדיוק כאן, ומכאן הוא נפתח ומתקרב אל הסימן. */
      /* עוד קצת ימינה מנקודת הנגיעה: בקפיצה הראשונה הגלגל עדיין
         עולה גבוה, וכך האות הראשונה יוצאת רק כשהוא כבר יורד. */
      GAP = Math.max(0, x - wordLeft) + R * 0.7;
      wordT = 0;
      wordDur = Math.max(0.55, Math.min(1.1, GAP / 90));
    }
    if (stageT >= 0 && stageT < stageDur) stageT = Math.min(stageDur, stageT + h);
    if (wordT >= 0 && wordT < wordDur) wordT = Math.min(wordDur, wordT + h);
  }

  /* מקום רציף על הרצועה לפי עומק המעיכה. בדרך פנימה מחפשים בין
     הקדרים הראשונים, בדרך החוצה בין האחרונים — לאותו עומק יש שתי
     צורות שונות, וזה בדיוק מה שנותן לפגיעה את ההרגשה של חומר ולא של
     תמונה שנמתחת. המספר שיוצא הוא שבור: בין שני קדרים. */
  function hitAt(q, closing) {
    var lo = closing ? 0 : 3, hi = closing ? 3 : HIT_D.length - 1;
    for (var i = lo; i < hi; i++) {
      var a = HIT_D[i], b = HIT_D[i + 1];
      if ((q - a) * (q - b) <= 0) return a === b ? i : i + (q - a) / (b - a);
    }
    return q > HIT_D[closing ? hi : lo] ? (closing ? hi : lo) : (closing ? lo : hi);
  }

  /* העיוות הנראה חי בדיוק כל עוד יש מגע: הוא נולד בנגיעה ונגמר
     ברגע שהגלגל עוזב. אין לו המשך באוויר. */
  var penPrev = 0, inHit = false, hitMax = 0, hitF = 0;

  function paint(dt) {
    dt = dt || 0;
    /* בצמיג מעט מרוקן יש משטח מגע קטן גם סתם כך, ולכן במגע יש רצפה
       למעיכה הנראית — באוויר הגלגל עגול לגמרי, כמו צמיג אמיתי.
       מה שנחתך מעבר לחדירה הפיזיקלית מוחזר בשקיעת הציר, אחרת
       הגלגל היה מרחף מעל הקרקע בדיוק באותו שיעור. */
    var flat = pen > 0 ? Math.max(pen, R * FLAT0) : 0;

    /* מכה של ממש — מציגים את הרנדר. כל עוד הגומי עוד נמעך התא מונח
       לפי המשטח ולא לפי הציר, כי בתוכו הגלגל כבר מעוך בדיוק כמו
       שצריך; אחר כך, כשהוא כבר באוויר ורק מתיישר, התא נצמד לגלגל.
       המכה עצמה קצרה מפריים, ולכן העיוות הנראה אינו רגעי: הוא נכנס
       בקצב קצוב עד מלוא המעיכה שהייתה באמת, ומשם מתיישר בחזרה. */
    var pk = penPk; penPk = 0;               // העומק הגדול ביותר מאז הציור הקודם
    var deep = pk > R * HIT_ON;
    if (deep) {
      if (!inHit) { inHit = true; hitMax = 0; }
      if (pk > hitMax) hitMax = pk;
      if (pk >= penPrev) {
        /* נכנסים: הצורה נגזרת ישירות מהמעיכה העמוקה ביותר שהייתה
           מאז הציור הקודם, כדי שמכה שנופלת בין שני פריימים לא
           תיעלם. */
        hitF = hitAt(Math.min(pk / R, HIT_MAX), true);
      } else {
        /* יוצאים: לפי המעיכה שיש ברגע הזה ממש — לא לפי השיא שהיה
           בתוך הפריים, אחרת הפריים האחרון של המגע היה מראה גומי
           שעוד מנופח, ומיד אחריו גלגל עגול, וזאת קפיצה שרואים.
           מה שנשאר מהמעיכה נמתח על טווח ההתאוששות של הרנדר, ולכן
           הצורה עוקבת אחרי העומק האמיתי ומגיעה לעגול בדיוק כשהמגע
           נגמר. */
        hitF = hitAt(HIT_MAX * Math.max(0, pen) / Math.max(hitMax, 1e-6), false);
      }
    } else {
      inHit = false;
    }
    var showHit = deep;

    /* טשטוש תנועה. הגלגל עובר בשיא הנפילה כרבע מהקוטר שלו בין
       פריים לפריים; בלי טשטוש העין רואה סדרת קפיצות ולא נפילה.
       ברגע המגע מבטלים אותו: שם התמונה צריכה להיות חדה, אחרת
       המעיכה נמרחת ונעלמת. */
    if (mblur) {
      var bx = showHit ? 0 : Math.min(MB_MAX, Math.abs(vx) * dt * MB);
      var by = showHit ? 0 : Math.min(MB_MAX, Math.abs(vy) * dt * MB);
      if (bx < 0.2) bx = 0;
      if (by < 0.2) by = 0;
      mblur.setAttribute('stdDeviation', bx.toFixed(2) + ' ' + by.toFixed(2));
      wheel.style.filter = (bx || by) ? 'url(#spMB)' : '';
    }

    hit.style.display = showHit ? 'block' : 'none';
    cut.style.display = showHit ? 'none' : '';
    if (showHit) {
      /* התא הקרוב ביותר מצויר כמו שהוא, ואת השארית הקטנה — ההפרש
         בין הצורה שלו לצורה שצריכה להיות ברגע הזה — משלימים במתיחה
         סביב קו הקרקע. ככה המעבר בין תא לתא רציף, והפיקסלים נשארים
         חדים ושל הרנדר, בלי לערבב שני תאים זה בזה. */
      var foot = (deep || hitS === 1) ? surfPk : y + R;
      var i0 = Math.floor(hitF), fr = hitF - i0;
      var i1 = Math.min(HIT_D.length - 1, i0 + 1);
      var k = Math.min(HIT_N - 1, fr < 0.5 ? i0 : i1);
      var qt = HIT_D[i0] + (HIT_D[i1] - HIT_D[i0]) * fr;   // העומק המדויק עכשיו
      var wt = HIT_W[i0] + (HIT_W[i1] - HIT_W[i0]) * fr;   // והרוחב המדויק
      var cell = 2 * R * HIT_CELL;
      var gy = foot - (y - R);                 // קו הקרקע בתוך המסגרת
      hit.style.backgroundPosition = (k * 100 / (HIT_N - 1)).toFixed(3) + '% 0';
      hit.style.top = (gy + HIT_FOOT * (R / 116.5) - cell).toFixed(2) + 'px';
      hit.style.transform = 'translateX(-50%) scale('
        + (wt / HIT_W[k]).toFixed(4) + ',' + ((2 - qt) / (2 - HIT_D[k])).toFixed(4) + ')';
      /* שכבת האור נצמדת בדיוק לגלגל שמצויר עכשיו: תחתיתה על קו
         הקרקע, גובהה כגובה הגלגל המעוך, רוחבה כרוחבו. ככה זה אותו
         אור ממש כמו בגלגל המתגלגל, ואין קפיצה ברגע המגע. */
      if (lite) {
        lite.style.transform = 'translateY(' + (gy - 2 * R).toFixed(2) + 'px) scale('
          + wt.toFixed(4) + ',' + (1 - qt / 2).toFixed(4) + ')';
      }
      wheel.style.transform = 'translate(' + (x - R) + 'px,' + (y - R) + 'px)';
      penPrev = pk;
      paintRest();
      return;
    }
    penPrev = pk;
    wheel.style.transform = 'translate(' + (x - R) + 'px,' + (y - R + flat - pen) + 'px)';
    cut.style.clipPath = flat > 0.12 ? 'inset(0 0 ' + flat.toFixed(2) + 'px 0)' : 'none';
    // הסיבוב סביב הציר, ומעליו הנטייה וההיגוי של ההתנודדות
    var sp = 'rotateX(' + lean.toFixed(4) + 'rad) rotateY('
      + steer.toFixed(4) + 'rad) rotate(' + rot.toFixed(4) + 'rad)';
    // הגומי שנמעך לגובה מתרחב לרוחב; הרוחב נמדד במסך ולכן אחרי הסיבוב
    var wide = (1 + BULGE * flat / R).toFixed(4);
    if (tyre) tyre.style.transform = 'scaleX(' + wide + ') ' + sp;
    if (rim) rim.style.transform = sp;
    // שכבת האור מתרחבת עם הגומי אבל לא מסתובבת איתו
    if (lite) lite.style.transform = 'scaleX(' + wide + ')';


    paintRest();
  }

  function paintRest() {
    /* הצל תמיד על הרצפה — גם כשהגלגל עוד מעל הסימן. ככה הוא לא
       קופץ מטה ברגע שהגלגל יורד מהכתר, והוא מתנהג כמו צל אמיתי:
       ככל שהגוף גבוה יותר הוא רחב, בהיר ורך יותר. */
    var up = Math.max(0, ground - (y + R)) / (R * 3);
    var near = 1 / (1 + 2.2 * up * up);
    if (shade) {
      shade.style.opacity = (0.05 + 0.30 * near).toFixed(3);
      shade.style.transform = 'translate(' + (x - R) + 'px,' + (ground - SHH) + 'px)'
        + ' scale(' + (1.60 - 0.78 * near).toFixed(3) + ',' + (0.45 + 0.62 * near).toFixed(3) + ')';
    }
    // הגומי לוחץ את הסימן לגובה והוא מתרחב מעט לרוחב
    var k = Math.max(-0.02, Math.min(0.05, md / markH));
    mark.style.transform = 'scaleY(' + (1 - k).toFixed(4) + ') scaleX(' + (1 + k * 0.28).toFixed(4) + ')';

    if (stageT >= 0) {
      stageE = easeOut(stageT / stageDur);
      stage.style.transform = 'translateX(' + (-SHIFT * stageE).toFixed(2) + 'px)';
    }
    if (wordT >= 0) wordE = easeInOut(wordT / wordDur);
    revealWord();
  }

  function step(now) {
    if (!last) last = now;
    var dt = Math.min((now - last) / 1000, 1 / 30);
    last = now;
    var n = Math.max(1, Math.ceil(dt / H)), h = dt / n;
    for (var i = 0; i < n; i++) sub(h);
    paint(dt);
    if (x - R > exitX) { done = true; return; }
    if (!done) requestAnimationFrame(step);
  }

  paint();
  requestAnimationFrame(step);
})();
